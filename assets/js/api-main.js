// Creating section for currency exchange rates and submit button

var bodyElement = document.querySelector('body');
var sectionElement = document.createElement('section');
sectionElement.setAttribute('id', 'currency-exchange');
sectionElement.textContent("hello")

bodyElement.appendChild(sectionElement);
